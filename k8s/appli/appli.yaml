kind: Namespace
apiVersion: v1
metadata:
  name: cesi1
  labels:
    kubernetes.io/metadata.name: cesi1
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ema-demo-java
  namespace: cesi1
  labels:
    app: ema-demo-java
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ema-demo-java
  template:
    metadata:
      labels:
        app: ema-demo-java
    spec:
      containers:
        - name: ema-demo-java
          image: emanzat/ema-demo-java:c58d8177acfad8c20e751bd0944ef875d18fad20
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: SPRING_MONGODB_URI
              value: "mongodb://mongodb.mongodb.svc.cluster.local:27017/demo"
            - name: SPRING_MONGODB_DATABASE
              value: "demo"
            - name: MONGODB_COLLECTION_NAME
              value: "personse"
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: ema-demo-java-service
  namespace: cesi1
  labels:
    app: ema-demo-java
spec:
  type: ClusterIP
  selector:
    app: ema-demo-java
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ema-demo-java-ingress
  namespace: cesi1
  annotations:
    # Pour K3s avec Traefik
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  rules:
    - host: cesi1.beincloud.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ema-demo-java-service
                port:
                  number: 80